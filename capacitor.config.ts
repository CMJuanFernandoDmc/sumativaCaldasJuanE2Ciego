import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'sumativaCaldasJuanE2Ciego',
  webDir: 'www'
};

export default config;
